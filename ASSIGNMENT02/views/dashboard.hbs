<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
    </style>
    <title>Animated Splash Page</title>
</head>
<body>
    {{#if blogs.length}}
    <ul>
      {{#each blogs}}
        <li>
          <h2>{{blogname}}</h2>
          <p>{{blogcontent}}</p>
        </li>
      {{/each}}
    </ul>
    {{else}}
        <p>No blogs available.</p>
    {{/if}}
    <div>
    </div>
    <div class="splash-container">
        <h1>My Blog Page</h1>
         <form action="/dashboard" method="post">
            <div>
            <label for="blogname">Blog Title:</label>
            <input type="text" id="blogname" name="blogname" required><br /><br />
            </div>
            <label for="blogcontent">Blog Content:</label>
            <textarea rows = "5" cols = "60" id="blogcontent" name = "blogcontent"/></textarea><br>
            <div><button type="submit">Submit Blog</button></div>
        </form>
       
    </div>
</body>
</html>
